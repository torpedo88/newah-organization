/**
 * Food is now free text — a registrant says whether they brought something and
 * describes it — so the old fixed option list and donation tiers are gone.
 * Donation presets live in lib/constants/event.ts.
 */
export {};
